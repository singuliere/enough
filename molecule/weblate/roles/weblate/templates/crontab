*/5 * * * * cd /srv/weblate; sudo docker-compose -f docker-compose-securedrop-club.yml run --rm weblate update_index
@daily cd /srv/weblate; sudo docker-compose -f docker-compose-securedrop-club.yml run --rm weblate cleanuptrans
@hourly cd /srv/weblate; sudo docker-compose -f docker-compose-securedrop-club.yml run --rm weblate commit_pending --all --age=1
