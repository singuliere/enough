---
- name: install weblate
  hosts: weblate-host
  vars:
    domain: 'securedrop.club'

  roles:
    - { role: ansible-role-docker, docker_install_compose: true }
    - { role: sexy-debian }
    - { role: icinga2_client }
    - { role: weblate }
  become: True
