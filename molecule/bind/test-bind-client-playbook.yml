---
- name: prepare test client
  hosts: bind-client-host
  become: True

  tasks:
    - name: apt-get install dnsutils
      apt:
        name: "{{ item }}"
        state: present
      with_items:
        - dnsutils
