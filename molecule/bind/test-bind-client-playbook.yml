---
- name: prepare test client
  hosts: bind_client_host
  become: True

  tasks:
    - name: apt-get install dnsutils
      apt:
        name: "{{ item }}"
        state: present
      with_items:
        - dnsutils
