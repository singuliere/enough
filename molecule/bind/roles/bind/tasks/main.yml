---

- name: BIND Install
  package:
    pkg: [python-netaddr, bind9, bind9utils, dnsutils]
    state: present

- name: Create serial, based on last two digits of year, month, day, and hour
  command: date +%y%m%d%H
  register: bind_timestamp
  changed_when: false
  run_once: true

- name: BIND master config file allow recursion
  copy:
    content: |
      allow-recursion {
      };
    dest: /etc/bind/named.conf.allow-recursion
    force: no
    owner: root
    group: bind
    mode: '0640'

- name: BIND master config file
  template:
    src: named.conf.j2
    dest: /etc/bind/named.conf
    owner: root
    group: bind
    mode: '0640'
    validate: 'named-checkconf %s'
  notify: restart bind

- name: dump dynamic zones to disk, and remove their journal files
  shell: rndc sync -clean || true

- name: BIND zone file
  template:
    src: zone.conf.j2
    dest: "/var/cache/bind/{{ bind_zone_name }}"
    owner: root
    group: bind
    mode: '0640'
    validate: 'named-checkzone -d {{ bind_zone_name }} %s'
  notify: restart bind
