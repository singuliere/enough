---
- name: setup DNS server
  hosts: bind-host
  become: True

  vars:
    domain: 'securedrop.club'

  roles:
    - role: icinga2_client

    - role: monitoring-bind
      bind_zone_name: '{{ domain }}'
