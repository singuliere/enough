---
driver:
  name: openstack
lint:
  name: yamllint
platforms:
  - name: demo-host
    flavor: "s1-2"
    image: "Ubuntu 17.10"
provisioner:
  name: ansible
  lint:
    name: ansible-lint
  env:
    # https://github.com/metacloud/molecule/issues/1008 for why ../../.. and ../ only
    ANSIBLE_ROLES_PATH: roles:../../../infrastructure/roles:../postfix/roles:../bind/roles:../icinga/roles:../backup/roles:../misc/roles
  inventory:
    links:
      # Path is relative to .molecule folder
      group_vars: ../../../inventory/group_vars
      host_vars:  ../../../inventory/host_vars
scenario:
  name: demo
  test_sequence:
    - destroy
    - create
    - converge
    - verify
    - destroy
verifier:
  name: testinfra
  options:
    v: True
    s: True
  lint:
    name: flake8
