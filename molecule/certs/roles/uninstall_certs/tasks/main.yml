---
- name: Uninstall certs
  file:
    path: "/usr/local/share/ca-certificates/infrastructure/{{ item | replace('.pem', '.crt') }}"
    state: absent
  with_items: "{{ certs }}"
  notify: update certs
