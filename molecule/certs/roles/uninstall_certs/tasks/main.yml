---
- name: Uninstall certs
  file:
    path: "/usr/local/share/ca-certificates/securedrop.club/{{ item | replace('.pem', '.crt') }}"
    state: absent
  with_items: "{{ certs }}"
  notify: update certs
