---
driver:
  name: openstack
lint:
  name: yamllint
platforms:
  - name: ansible_host
    flavor: "s1-2"
    groups:
      - ansible_group
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: ansible
  test_sequence:
    - destroy
    - create
    - converge
    - verify
    - destroy
verifier:
  name: testinfra
  lint:
    name: flake8
