---
- name: Create
  hosts: localhost
  connection: local
  gather_facts: False
  vars:
    molecule_yml: "{{ lookup('file', 'molecule/infrastructure/molecule.yml') | from_yaml }}"
    user: debian
    ssh_port: 22
  vars_files:
    - group_vars/all/secrets.yml
  roles:
    - { role: vm, state: present }
