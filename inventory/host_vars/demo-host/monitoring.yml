---
monitoring_host_vars:
  - "/* Enlarge delay before service notification */"
  - vars.service_template = "delayed-notification-service"
  - "/* Enlarge delay before host notification */"
  - max_check_attempts = 15
