#
# Machines in the pets groups are long lived and contain data that
# needs to be saved.
#
pets:
  hosts:
    gitlab-host:
    weblate-host:
    packages-host:
    chat-host:
    forum-host:

enough:
  hosts:
    cloud-host:
    wereport-host:

wazuh_agent:
  hosts:
    bind-host:
    chat-host:
    forum-host:
    gitlab-host:
    icinga-host:
    packages-host:
    postfix-host:
    runner-host:
    weblate-host:
